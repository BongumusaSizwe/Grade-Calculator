<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Courses" />
        <activity android:name=".MC" />
        <activity android:name=".LINEAR_ALGEBRA" />
        <activity android:name=".selected_c" />
        <activity android:name=".MyCourses" />
        <activity android:name=".DBF" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MOBILE_COMPUTING" />
        <activity android:name=".Remove_Courses"></activity>
    </application>

</manifest>